server:
  port: 8080

spring:
  application:
    name: myapi

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/mydb
    username: seulgi
    password: 1111

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    sql: debug
    org:
      springframework:
        security:
          web: trace
